{
  "name": "macos-keychain-passwords",
  "version": "0.0.5",
  "description": "Allow access the macOS X Keychain via the OS X Security Framework native API",
  "homepage": "https://github.com/ecerulm/macos-keychain-passwords",
  "main": "index.js",
  "scripts": {
    "install": "node-gyp-build",
    "prebuild": "prebuildify --napi",
    "reuse-lint": "pipx run reuse lint",
    "reuse-annotate": "pipx run reuse annotate --year 2023 --copyright 'Ruben Laguna <ruben.laguna@gmail.com>' --license GPL-3.0-or-later",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "mac",
    "macos",
    "security",
    "keychain",
    "password",
    "management",
    "passwords"
  ],
  "author": "Ruben Laguna <ruben.laguna@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "node-gyp-build": "^4.6.0"
  },
  "devDependencies": {
    "eslint": "^8.37.0"
  }
}
